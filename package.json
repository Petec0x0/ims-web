{
    "scripts": {
      "heroku-prebuild": "cd client && npm install && npm run build && mv build ../server/client && cd ../server && npm install",
      "start": "cd server && npm start"
    }
  }
  